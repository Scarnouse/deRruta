<section class="sidebar">

    <!-- Sidebar user panel (optional) -->
    <div class="user-panel">
        <div class="pull-left image">
            <img src="{{ user.getWebPathImage() }}" class="img-circle" alt="User Image" />
        </div>
        <div class="pull-left info">
            <p>{{ user.getFirstName() }}</p>
            <!-- Status -->
            <a href="#"><i class="fa fa-circle text-success"></i> Online</a>
        </div>
    </div>

    <!-- Sidebar Menu -->
    <ul class="sidebar-menu">
        <li class="header">APLICACIONES</li>
        <!-- Optionally, you can add icons to the links -->
        {#<li><a href="{{ path('cities') }}"><i class="fa fa-bandcamp"></i><span>Ciudades</span></a></li>#}
        <li><a href="{{ path('my_routes') }}"><i class="fa fa-map-o"></i><span>Mis Rutas</span></a></li>
        <li><a href="{{ path('routes') }}"><i class="fa fa-map"></i><span>Rutas</span></a></li>

    </ul><!-- /.sidebar-menu -->
</section>